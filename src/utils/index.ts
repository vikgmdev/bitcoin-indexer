import fetchJSON from './fetch-json';
import generateRPCRequest from './rpc-request';

export { default as logger } from './logger';

const Utils = {
  fetchJSON,
  generateRPCRequest,
};

export default Utils;
